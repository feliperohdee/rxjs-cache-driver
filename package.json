{
  "name": "smallorange-rxjs-cache-driver",
  "version": "1.1.3",
  "description": "Simple pluggable reactive cache driver powered by RxJS.",
  "main": "index.js",
  "scripts": {
    "test": "NODE_ENV=test ./node_modules/.bin/_mocha spec --recursive --watch --timeout 5000",
    "coverage": "NODE_ENV=test ./node_modules/.bin/istanbul cover -x spec/** ./node_modules/.bin/_mocha -- 'spec' --recursive --timeout 5000",
    "deploy": "npm version patch -m 'Bump %s' && git push origin master && npm publish"
  },
  "author": "Felipe Rohde",
  "license": "ISC",
  "repository": {
    "type": "git",
    "url": "https://github.com/feliperohdee/smallorange-rxjs-cache-driver"
  },
  "homepage": "https://smallorange.co",
  "dependencies": {
    "rxjs": "^5.5.6"
  },
  "devDependencies": {
    "chai": "^4.1.2",
    "istanbul": "^0.4.5",
    "mocha": "^4.1.0",
    "sinon": "^4.1.3",
    "sinon-chai": "^2.14.0"
  }
}
